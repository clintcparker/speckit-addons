# Spec Kit project state — this repo publishes add-ons, it does not consume them.
# A scratch `specify init` for testing should never be committed.
.specify/

# Python (scripts/)
__pycache__/
*.py[cod]
.venv/
venv/

# macOS
.DS_Store

# Editors
.idea/
*.swp
