schema_version: "1.0"

extension:
  id: worktrees
  name: "Worktrees"
  version: "2.4.0"
  description: "Default-on worktree isolation for parallel agents — worktree-first flow, sibling or nested layout"
  author: "clintcparker"
  repository: "https://github.com/clintcparker/speckit-addons"
  license: "MIT"

requires:
  speckit_version: ">=0.4.0"
  tools:
    - name: git
      required: true

provides:
  commands:
    - name: speckit.worktrees.create
      file: commands/speckit.worktrees.create.md
      description: "Spawn an isolated git worktree for a feature branch"
    - name: speckit.worktrees.list
      file: commands/speckit.worktrees.list.md
      description: "Dashboard of all active worktrees with spec-artifact and task progress"
    - name: speckit.worktrees.clean
      file: commands/speckit.worktrees.clean.md
      description: "Remove merged, orphaned, or stale worktrees and reclaim disk space"

  config:
    - name: "worktree-config.yml"
      template: "worktree-config.yml"
      description: "Worktree layout and behavior configuration"
      required: false

hooks:
  before_specify:
    command: speckit.worktrees.create
    optional: false
    priority: 20
    description: "Create the feature branch inside a new worktree and work there"

tags:
  - "worktree"
  - "git"
  - "parallel"
  - "isolation"
  - "agents"

config:
  defaults:
    layout: "sibling"
    auto_create: true
    sibling_pattern: "{{repo}}--{{branch}}"
    dotworktrees_dir: ".worktrees"
    # Read as the base layer beneath the deployed worktree-config.yml, which can
    # legitimately be absent — so the default has to be correct in both places.
    lock_ttl_minutes: 240
